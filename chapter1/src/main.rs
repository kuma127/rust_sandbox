mod arithmetric;
mod comparison;
mod fizbuzz;
mod pattern_match;
mod linked_list;
fn main() {
    // arithmetric::symbol();
    // arithmetric::methods(10, 3);
    //arithmetric::overflow();
    //comparison::methods(10, 10);
    //fizbuzz::fizzbuzz();
    //pattern_match::pattern_match("tes");
    linked_list::create_list();
}
