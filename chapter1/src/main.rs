mod arithmetric;
mod comparison;
mod fizbuzz;
fn main() {
    // arithmetric::symbol();
    // arithmetric::methods(10, 3);
    //arithmetric::overflow();
    //comparison::methods(10, 10);
    fizbuzz::fizzbuzz();
}
